<div>
    <>
        {
        match.response[0]?.players[1]?.players.map(homePlayer =>
        <div key={homePlayer.player.id} className='flex items-center'>
            {/*
            <Image src={homePlayer.player.photo} width={100} height={100} alt='' /> */}
            <p>{homePlayer.player.id}</p>
        </div>
        )
        }
    </>
</div>

<div className='bg-sky-200 rounded-lg w-fit px-6 py-3 mx-auto mt-10 flex'>
    <div className='mr-5 font-bold'>
        <p>Date:</p>
        <p>Time:</p>
        <p>Venue:</p>
    </div>
    <div>
        <p>{date}</p>
        <p>{match.response[0].fixture.date.slice(11, 16)}</p>
        <p>{match.response[0].fixture.venue.name}, {match.response[0].fixture.venue.city}</p>
    </div>
</div>